# 复制为 config.yaml 并填入你的真实值；容器部署可完全不用此文件（纯环境变量）
addr: ":8080"
mysql_dsn: "user:password@tcp(127.0.0.1:3306)/pluto_feed?parseTime=true&charset=utf8mb4&loc=Local"
jwt_secret: "换成随机长字符串"
jwt_access_ttl_minutes: 30
jwt_refresh_ttl_days: 7
redis_addr: "127.0.0.1:6379"
rabbit_url: "amqp://guest:guest@127.0.0.1:5672/"
